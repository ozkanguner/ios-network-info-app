{
  "name": "NetworkInfoApp",
  "version": "1.0.0",
  "description": "iOS Network Info App in React Native",
  "main": "index.js",
  "scripts": {
    "start": "npx react-native start",
    "android": "npx react-native run-android",
    "ios": "npx react-native run-ios",
    "build:ios": "npx react-native bundle --platform ios --dev false --entry-file index.js --bundle-output ios/main.jsbundle"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-native": "^0.72.0",
    "@react-native-community/netinfo": "^10.0.0",
    "react-native-device-info": "^10.11.0"
  },
  "devDependencies": {
    "@react-native-community/cli": "^11.3.0"
  }
}
