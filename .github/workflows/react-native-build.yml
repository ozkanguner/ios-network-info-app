name: React Native Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-android:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '24'
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Set up Java
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'
    
    - name: Set up Android SDK
      uses: android-actions/setup-android@v3
    
    - name: Build Android APK
      run: |
        cd android
        ./gradlew assembleRelease
        cd ..
    
    - name: Upload Android APK
      uses: actions/upload-artifact@v4
      with:
        name: android-apk
        path: android/app/build/outputs/apk/release/
    
    - name: Upload build logs
      uses: actions/upload-artifact@v4
      with:
        name: android-build-logs
        path: android/app/build/outputs/

  build-ios:
    runs-on: macos-14
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '24'
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Install iOS dependencies
      run: |
        cd ios
        pod install
        cd ..
    
    - name: Build iOS
      run: |
        cd ios
        xcodebuild -workspace NetworkInfoAppRN.xcworkspace \
                   -scheme NetworkInfoAppRN \
                   -configuration Release \
                   -destination generic/platform=iOS \
                   clean build
        cd ..
    
    - name: Upload iOS build
      uses: actions/upload-artifact@v4
      with:
        name: ios-build
        path: ios/build/

  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '24'
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Run tests
      run: npm test
    
    - name: Run linting
      run: npm run lint
    
    - name: Upload test results
      uses: actions/upload-artifact@v4
      with:
        name: test-results
        path: |
          coverage/
          test-results/

  deploy:
    needs: [build-android, build-ios, test]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    steps:
    - name: Download all artifacts
      uses: actions/download-artifact@v4
      with:
        path: artifacts/
    
    - name: Create release
      uses: softprops/action-gh-release@v2
      with:
        files: |
          artifacts/android-apk/**/*.apk
          artifacts/ios-build/**/*
        tag_name: v${{ github.run_number }}
        name: Release v${{ github.run_number }}
        body: |
          ## 🚀 React Native Network Info App Release
          
          ### 📱 Android APK
          - APK dosyaları artifacts/android-apk/ klasöründe
          
          ### 🍎 iOS Build
          - iOS build dosyaları artifacts/ios-build/ klasöründe
          
          ### ✅ Test Sonuçları
          - Test sonuçları artifacts/test-results/ klasöründe
          
          ### 🔄 Build #${{ github.run_number }}
          - Commit: ${{ github.sha }}
          - Branch: ${{ github.ref_name }}
          - Triggered by: ${{ github.actor }}
